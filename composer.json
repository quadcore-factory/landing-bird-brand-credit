{
  "name": "landingbird/brand-credit",
  "description": "A configurable whitelabel brand credit for WordPress.",
  "type": "wordpress-plugin",
  "license": "GPL-2.0-or-later",
  "require": { "php": ">=7.4" },
  "scripts": {
    "lint": "find . -path './.git' -prune -o -name '*.php' -type f -print0 | xargs -0 -n1 php -l",
    "test": "php tests/test.php"
  }
}
